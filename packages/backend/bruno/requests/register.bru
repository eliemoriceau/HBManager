meta {
  name: Register
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com",
    "password": "password"
  }
}
